---
const { items = [] } = Astro.props;
---

<dl class="metalist">
  {items.map((it) => (
    <>
      <dt>{it.k}</dt>
      <dd>{it.href ? <a href={it.href}>{it.v}</a> : it.v}</dd>
    </>
  ))}
</dl>

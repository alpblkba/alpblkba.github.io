---
const { label, href = '#', external = false, current = false } = Astro.props;
---
<a
  class="navlink"
  href={href}
  aria-current={current ? 'page' : undefined}
  target={external ? '_blank' : undefined}
  rel={external ? 'noreferrer' : undefined}
>{label}</a>
